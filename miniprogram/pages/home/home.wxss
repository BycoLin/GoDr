.home-page {
  overflow-x: hidden;
  width: 100%;
}

page {
  overflow-x: hidden;
}

.home {
  position: relative;
  overflow-x: hidden;
  box-sizing: border-box;
  padding-bottom: 16rpx;
}

.load-hint {
  position: relative;
  z-index: 2;
  margin-bottom: 16rpx;
  padding: 16rpx 20rpx;
  border-radius: 20rpx;
  background: #fff5f3;
  border: 3rpx solid #ffd0c8;
  color: #8a3b32;
  font-size: 26rpx;
  font-weight: 800;
}

.deco {
  position: absolute;
  pointer-events: none;
  z-index: 0;
  opacity: 0.35;
}

.deco-cloud {
  width: 140rpx;
  height: 56rpx;
  background: #fff;
  border-radius: 999rpx;
}

.deco-cloud::before,
.deco-cloud::after {
  content: '';
  position: absolute;
  background: #fff;
  border-radius: 50%;
}

.deco-cloud::before {
  width: 60rpx;
  height: 60rpx;
  top: -28rpx;
  left: 24rpx;
}

.deco-cloud::after {
  width: 80rpx;
  height: 80rpx;
  top: -40rpx;
  right: 20rpx;
}

.deco-cloud.a {
  top: 20rpx;
  right: 0;
}

.deco-cloud.b {
  top: 280rpx;
  left: 0;
  transform: scale(0.7);
  opacity: 0.22;
}

.deco-star {
  top: 120rpx;
  left: 40rpx;
  width: 24rpx;
  height: 24rpx;
  background: #ffc93c;
  border-radius: 4rpx;
  transform: rotate(20deg);
  opacity: 0.4;
}

.status-row,
.subject-pills,
.grade-pills,
.scene-card,
.goal-card,
.sec-title,
.feature-list,
.adventure-dock,
.more-list {
  position: relative;
  z-index: 1;
}

.status-row {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin-bottom: 20rpx;
}

.avatar {
  width: 100rpx;
  height: 100rpx;
  border-radius: 50%;
  overflow: hidden;
  flex-shrink: 0;
  background: #fff4e4;
  border: 4rpx solid #fff;
  box-shadow: 0 8rpx 0 rgba(26, 20, 48, 0.08);
  animation: pet-bounce 1.8s ease-in-out infinite;
}

.avatar-img {
  width: 100%;
  height: 100%;
  display: block;
}

@keyframes pet-bounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-8rpx); }
}

.status-mid {
  flex: 1;
  min-width: 0;
}

.hello {
  font-size: 34rpx;
  font-weight: 900;
  color: #1a1430;
}

.hello-sub {
  margin-top: 4rpx;
  font-size: 26rpx;
  font-weight: 800;
  color: #e08a12;
}

.status-chips {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
  align-items: flex-end;
}

.chip {
  background: #fff;
  border: 3rpx solid #ece7df;
  border-radius: 999rpx;
  padding: 8rpx 16rpx;
  font-size: 24rpx;
  font-weight: 900;
  color: #1a1430;
  box-shadow: 0 4rpx 0 rgba(26, 20, 48, 0.06);
}

.chip.fire {
  color: #ff6f61;
  border-color: #ffd0c8;
  background: #fff5f3;
}

.subject-pills {
  display: flex;
  gap: 16rpx;
  margin-bottom: 12rpx;
}

.subject-head {
  position: relative;
  z-index: 1;
  margin-bottom: 8rpx;
}

.subject-swipe-tip {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
  margin-bottom: 12rpx;
  padding: 0 4rpx;
}

.subject-dots {
  display: flex;
  align-items: center;
  gap: 10rpx;
}

.subject-dot {
  width: 14rpx;
  height: 14rpx;
  border-radius: 50%;
  background: #d8d2e0;
  transition: width 0.2s ease, background 0.2s ease;
}

.subject-dot.on {
  width: 28rpx;
  border-radius: 999rpx;
}

.subject-dot.on.cn {
  background: #22b8a8;
}

.subject-dot.on.math {
  background: #5b8cff;
}

.subject-dot.on.en {
  background: #ff9f1c;
}

.subject-swipe-text {
  font-size: 22rpx;
  font-weight: 700;
  color: #9a93a8;
}

.subject-swiper {
  overflow: hidden;
}

.subject-scroll {
  width: 100%;
  box-sizing: border-box;
}

.subject-slide {
  box-sizing: border-box;
  padding-bottom: 16rpx;
}

.sp {
  flex: 1;
  text-align: center;
  min-height: 96rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 999rpx;
  background: #fff;
  border: 4rpx solid #ece7df;
  font-size: 32rpx;
  font-weight: 900;
  color: #4a4458;
  box-shadow: 0 8rpx 0 rgba(26, 20, 48, 0.06);
}

.sp:active {
  transform: translateY(4rpx);
  box-shadow: 0 4rpx 0 rgba(26, 20, 48, 0.06);
}

.sp.on.cn {
  background: #22b8a8;
  border-color: #22b8a8;
  color: #fff;
  box-shadow: 0 8rpx 0 #0f8f82;
}

.sp.on.math {
  background: #5b8cff;
  border-color: #5b8cff;
  color: #fff;
  box-shadow: 0 8rpx 0 #4470e0;
}

.sp.on.en {
  background: #ff9f1c;
  border-color: #ff9f1c;
  color: #fff;
  box-shadow: 0 8rpx 0 #e07f10;
}

.grade-pills {
  display: flex;
  gap: 14rpx;
  margin-bottom: 28rpx;
  flex-wrap: wrap;
}

.gp {
  min-height: 72rpx;
  padding: 0 28rpx;
  display: flex;
  align-items: center;
  border-radius: 999rpx;
  background: #fff;
  border: 3rpx solid #ece7df;
  font-size: 28rpx;
  font-weight: 800;
  color: #4a4458;
}

.gp.on {
  background: #ffc93c;
  border-color: #ffc93c;
  color: #5a4300;
  box-shadow: 0 6rpx 0 #e0ad20;
}

.gp-pre {
  font-size: 24rpx;
  padding: 0 20rpx;
}

.scene-card {
  border-radius: 40rpx;
  padding: 36rpx 32rpx 28rpx;
  color: #fff;
  margin-bottom: 20rpx;
  text-align: center;
  position: relative;
}

.scene-card.tone-cn {
  background: #22b8a8;
  box-shadow: 0 14rpx 0 #0f8f82;
}

.scene-card.tone-math {
  background: #5b8cff;
  box-shadow: 0 14rpx 0 #4470e0;
}

.scene-card.tone-en {
  background: #ff9f1c;
  box-shadow: 0 14rpx 0 #e07f10;
}

.scene-mark {
  width: 88rpx;
  height: 88rpx;
  margin: 0 auto 16rpx;
  border-radius: 28rpx;
  background: rgba(255, 255, 255, 0.28);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 40rpx;
  font-weight: 900;
}

.scene-title {
  font-size: 44rpx;
  font-weight: 900;
}

.scene-desc {
  margin-top: 8rpx;
  font-size: 26rpx;
  font-weight: 700;
  opacity: 0.92;
}

.scene-bar {
  margin: 22rpx auto 10rpx;
  width: 86%;
  height: 22rpx;
  background: rgba(255, 255, 255, 0.28);
  border-radius: 999rpx;
  overflow: hidden;
}

.scene-fill {
  height: 100%;
  background: #ffc93c;
  border-radius: 999rpx;
}

.scene-pct {
  font-size: 40rpx;
  font-weight: 900;
}

.goal-card {
  background: #fff;
  border: 3rpx solid #ece7df;
  border-radius: 32rpx;
  padding: 24rpx 28rpx 22rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 8rpx 0 rgba(26, 20, 48, 0.06);
}

.goal-card.done {
  border-color: #9ee5db;
  background: #effffb;
}

.goal-head {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 14rpx;
}

.goal-title-wrap {
  display: flex;
  align-items: center;
  gap: 12rpx;
  min-width: 0;
}

.goal-title {
  font-size: 30rpx;
  font-weight: 900;
  color: #1a1430;
}

.goal-scope {
  flex-shrink: 0;
  padding: 4rpx 12rpx;
  border-radius: 999rpx;
  background: #eef3ff;
  color: #5b8cff;
  font-size: 20rpx;
  font-weight: 800;
}

.goal-count {
  font-size: 28rpx;
  font-weight: 900;
  color: #22b8a8;
}

.goal-card.done .goal-count {
  color: #0f8f82;
}

.goal-bar {
  height: 18rpx;
  background: #efeaf4;
  border-radius: 999rpx;
  overflow: hidden;
}

.goal-fill {
  height: 100%;
  background: #22b8a8;
  border-radius: 999rpx;
  transition: width 0.25s ease;
}

.goal-card.done .goal-fill {
  background: #ffc93c;
}

.goal-tip {
  margin-top: 12rpx;
  font-size: 24rpx;
  font-weight: 700;
  color: #4a4458;
}

.sec-title {
  position: relative;
  z-index: 1;
  font-size: 32rpx;
  font-weight: 900;
  color: #1a1430;
  margin-bottom: 16rpx;
}

.feature-list {
  position: relative;
  z-index: 1;
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  margin-bottom: 24rpx;
}

.subject-slide .more-list {
  margin-top: 4rpx;
  padding-bottom: 8rpx;
}

.feature-item {
  display: flex;
  align-items: center;
  gap: 18rpx;
  min-height: 120rpx;
  background: #fff;
  border-radius: 32rpx;
  border: 3rpx solid #ece7df;
  padding: 20rpx 22rpx;
  box-shadow: 0 10rpx 0 rgba(26, 20, 48, 0.06);
}

.feature-item.q-duel {
  background: #f3f6ff;
  border-color: #c9d6ff;
}

.feature-item.q-sprint,
.feature-item.q-flash {
  background: #fff8ef;
  border-color: #ffe0a8;
}

.feature-item.q-teal,
.feature-item.q-exam {
  background: #effffb;
  border-color: #9ee5db;
}

.feature-item.q-coral,
.feature-item.q-review {
  background: #fff5f3;
  border-color: #ffd0c8;
}

.feature-icon {
  width: 80rpx;
  height: 80rpx;
  border-radius: 24rpx;
  background: #22b8a8;
  color: #fff;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 26rpx;
  font-weight: 900;
  flex-shrink: 0;
}

.q-duel .feature-icon { background: #5b8cff; }
.q-sprint .feature-icon,
.q-flash .feature-icon { background: #ff9f1c; }
.q-teal .feature-icon,
.q-exam .feature-icon { background: #22b8a8; }
.q-coral .feature-icon,
.q-review .feature-icon { background: #ff6f61; }

.feature-body {
  flex: 1;
  min-width: 0;
}

.feature-name {
  font-size: 32rpx;
  font-weight: 900;
  color: #1a1430;
}

.feature-tip {
  margin-top: 4rpx;
  font-size: 24rpx;
  font-weight: 700;
  color: #4a4458;
}

.feature-go {
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  background: #1a1430;
  color: #fff;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  font-weight: 900;
  flex-shrink: 0;
}

.more-list {
  display: flex;
  flex-direction: column;
  gap: 18rpx;
}

.more-item {
  display: flex;
  align-items: center;
  gap: 20rpx;
  min-height: 120rpx;
  background: #fff;
  border-radius: 32rpx;
  border: 3rpx solid #ece7df;
  padding: 20rpx 22rpx;
  box-shadow: 0 10rpx 0 rgba(26, 20, 48, 0.06);
  box-sizing: border-box;
}

button.more-item {
  width: 100%;
  margin: 0;
  line-height: normal;
  font-weight: normal;
  font-size: inherit;
  text-align: left;
}

button.more-item::after {
  border: none;
}

.more-item-active {
  transform: translateY(4rpx);
  box-shadow: 0 6rpx 0 rgba(26, 20, 48, 0.06);
  opacity: 0.96;
}

.more-icon {
  width: 80rpx;
  height: 80rpx;
  border-radius: 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 32rpx;
  font-weight: 900;
  color: #fff;
  flex-shrink: 0;
}

.e-review .more-icon { background: #ff6f61; }
.e-mono .more-icon { background: #ff9f1c; }

.e-review { background: #fff5f3; border-color: #ffd0c8; }
.e-mono { background: #fff8ef; border-color: #ffe0a8; }
.e-share { background: #f0fffb; border-color: #9ee5db; }
.e-share .more-icon { background: #22b8a8; }

.more-body {
  flex: 1;
  min-width: 0;
}

.more-name {
  font-size: 32rpx;
  font-weight: 900;
  color: #1a1430;
}

.more-tip {
  margin-top: 4rpx;
  font-size: 24rpx;
  font-weight: 700;
  color: #4a4458;
}

.more-go {
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  background: #1a1430;
  color: #fff;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  font-weight: 900;
  flex-shrink: 0;
}
