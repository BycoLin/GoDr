page {
  --color-bg: #e6fff6;
  --color-surface: #ffffff;
  --color-ink: #1a1430;
  --color-muted: #4a4458;
  --color-primary: #22b8a8;
  --color-primary-deep: #0f8f82;
  --color-primary-soft: #d6f8f2;
  --color-accent: #ff6f61;
  --color-accent-soft: #ffe2de;
  --color-sky: #5b8cff;
  --color-sky-soft: #e2ebff;
  --color-sun: #ffc93c;
  --color-sun-soft: #fff3c4;
  --color-grass: #5ecf7b;
  --color-danger: #e84b6a;
  --color-ok: #2fbf7e;
  --radius: 36rpx;
  --radius-lg: 48rpx;
  --shadow: 0 12rpx 0 rgba(26, 20, 48, 0.1);
  --gap: 28rpx;
  --tap-min: 120rpx;
  --font-hero: 48rpx;
  --font-title: 36rpx;
  --font-body: 32rpx;
  --font-sub: 26rpx;
}

page {
  background:
    radial-gradient(circle at 48% 100%, rgba(255, 176, 160, 0.42) 0%, transparent 42%),
    radial-gradient(circle at 8% 88%, rgba(255, 210, 120, 0.3) 0%, transparent 30%),
    radial-gradient(circle at 92% 92%, rgba(255, 190, 170, 0.26) 0%, transparent 28%),
    radial-gradient(circle at 16% 6%, rgba(64, 200, 180, 0.28) 0%, transparent 34%),
    radial-gradient(circle at 84% 4%, rgba(120, 210, 200, 0.22) 0%, transparent 30%),
    linear-gradient(180deg, #e6fff6 0%, #eef3ff 48%, #ffe8dc 100%);
  color: var(--color-ink);
  font-size: var(--font-body);
  min-height: 100vh;
  box-sizing: border-box;
  line-height: 1.45;
  font-family: "PingFang SC", "Noto Sans SC", "Microsoft YaHei", sans-serif;
}

.btn {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: var(--tap-min);
  padding: 0 48rpx;
  border-radius: 999rpx;
  font-size: var(--font-title);
  font-weight: 900;
  border: none;
  line-height: 1.2;
}

.btn::after {
  border: none;
}

.btn-primary {
  background: #22b8a8;
  color: #fff;
  box-shadow: 0 12rpx 0 #0f8f82;
}

.btn-primary:active {
  transform: translateY(6rpx);
  box-shadow: 0 6rpx 0 #0f8f82;
}

.btn-cta {
  width: 80%;
  margin: 0 auto;
  min-height: 140rpx;
  font-size: 40rpx;
  letter-spacing: 2rpx;
}

.btn-secondary {
  background: #fff;
  color: var(--color-primary-deep);
  border: 4rpx solid #9ee5db;
  box-shadow: 0 10rpx 0 rgba(15, 143, 130, 0.18);
}

.btn-secondary:active {
  transform: translateY(4rpx);
  box-shadow: 0 6rpx 0 rgba(15, 143, 130, 0.18);
}

.btn-ghost {
  background: transparent;
  color: var(--color-muted);
}

.card {
  background: #ffffff;
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  padding: 32rpx;
  border: 3rpx solid #ece7df;
  position: relative;
}

.safe-pad {
  padding: 32rpx 32rpx;
  padding-bottom: calc(64rpx + env(safe-area-inset-bottom));
}

.section-title {
  display: flex;
  align-items: center;
  gap: 12rpx;
  font-size: var(--font-title);
  color: var(--color-ink);
  margin: 8rpx 0 20rpx;
  font-weight: 900;
}

.section-title::before {
  content: '';
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  background: var(--color-sun);
  box-shadow: 8rpx 0 0 var(--color-accent), 16rpx 0 0 var(--color-sky);
}

/* 按压反馈通用 */
.pressable:active {
  transform: translateY(4rpx);
  opacity: 0.96;
}
